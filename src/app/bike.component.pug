<div class="mat-elevation-z8">
  table
    mat-table([datasource]="dataSource")
      ng-container(matColumnDef="name")
        th
          mat-header-cell(*matHeaderCellDef) name
        td
          mat-cell(*matCellDef="let element") {{element.name}}

      //- div.container(matColumnDef="maker")
      //-   th
      //-     mat-header-cell(*matHeaderCellDef) maker
      //-   td
      //-     mat-cell(*matCellDef="let element") {{element.name}}

      //- div.container(matColumnDef="weight")
      //-   th
      //-     mat-header-cell(*matHeaderCellDef) weight
      //-   td
      //-     mat-cell(*matCellDef="let element") {{element.name}}

      //- div.container(matColumnDef="class")
        th
          mat-header-cell(*matHeaderCellDef) class
        td
          mat-cell(matCellDef="let element") {{element.name}}

      tr
        mat-header-row(*matHeaderRowDef="displayedColumns")
      tr
        mat-row(*matRowDef="let row; columns: displayedColumns;")
  mat-paginator([pagesizeoptions]="[5, 10, 20]" showFirstLastButtons)

</div>